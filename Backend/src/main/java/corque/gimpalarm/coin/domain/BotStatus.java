package corque.gimpalarm.coin.domain;

public enum BotStatus {
    WAITING,
    ENTRY_SUBMITTING,
    ENTRY_PENDING,
    ENTERING,
    HOLDING,
    EXIT_SUBMITTING,
    EXIT_PENDING,
    EXITING,
    STOPPED,
    ERROR
}
